# velocidad2 = valocidad1 + g * tiempo
# altura = ( velocidad1 + velocidad2 / 2)
# altura = (velocidad1 * tiempo) + g * tiempo^2 / 2
# velocidad2^2 = velocidad1^2 + g * altura
# tiempo = sqrt(2 * altura / g)



# def tiempo_caida_libre(altura: int, masa: int) -> str:
#     #inicio
#         # Calculando el tiempo de caída libre de un cuerpo
#         # Variables: gravedad = 9.807 m/s2
#         # Parámetros: altura (int): posición inicial desde la que se dejara caer el objeto.
#                       masa (int): Masa del objeto del experimento.
#  # Retorno:
#  String: de la forma " El tiempo que tarda un balón de {masa}kg en caída libre desde una
#  altura de {altura}m en impactar en el suelo es de: {tiempo}seg"
# #fin
# pass

# def tiempo_caida_libre(altura: int, masa: int, operacion: float) -> str:
#     if == 'sqrt()':
#         result = tiempo_caida_libre(altura, masa)
#     return result


# def tiempo_caida_libre(altura: int, masa: int, operacion: float) -> str:
#     multiplicacion = '2 * altura'
#     return multiplicacion

# def tiempo_caida_libre(altura: int, masa: int, operacion: float) -> str:
#     division = 'multiplicacion / gravedad'
#     return division

# def tiempo_caida_libre(altura: int, masa: int, operacion: float) -> str:
#     raiz_cuadrada = sqrt(division)
#     return raiz_cuadrada

# ↓2♣↓↓Ë5§2§☻1¹¹
# altura = 10 
# masa = 85  
# print(altura, masa) 
    
# def tiempo_caida_libre(altura: int, masa: int) -> str:
#     multiplicacion = '2 * altura'
#     return multiplicacion

# def tiempo_caida_libre(altura: int, masa: int) -> str:
#     division = 'multiplicacion / gravedad'
#     return division

# def tiempo_caida_libre(altura: int, masa: int) -> str:
#     raiz_cuadrada = 'division / x'
#     return raiz_cuadrada
    
# def tiempo_caida_libre(altura: int, masa: int, operacion:str) -> str:
#     if operacion == '*':
#         result = tiempo_caida_libre.multiplicacion(2, altura)
#     elif operacion == '/':
#         result = tiempo_caida_libre.division(multiplicacion, gravedad)
#     elif operacion == 'sqrt()':
#         result = tiempo_caida_libre.raiz(division) 
#     return result


# from math import sqrt


# altura = 10
# masa = 85
# gravedad = 9.807 

# def tiempo_caida_libre(altura: int, masa: int) -> str:
#     multiplicar = '*'
#     if multiplicar == 2 * altura:   
#         return multiplicar
        
# def tiempo_caida_libre(altura: int, masa: int) -> str:
#     division = '//'
#     if division == 20 // gravedad:   
#         return division
        
# def tiempo_caida_libre(altura: int, masa: int) -> str:
#     raiz = 'x**(1/2)'
#     if raiz == '2.039359641072703 ** (1/2)':  
#         return raiz

# def tiempo_caida_libre(altura: int, masa: int) -> str:
#     multiplicar = '*'
#     if multiplicar == 2 * altura:   
#         return multiplicar
        
# def tiempo_caida_libre(altura: int, masa: int) -> str:
#     division = '/'
#     def tiempo_caida_libre(multiplicar):
#          if division == multiplicar / gravedad:   
#              return division
        
# def tiempo_caida_libre(altura: int, masa: int) -> str:
#     raiz = 'x**(1/2)'
#     def tiempo_caida_libre(division):
#          if raiz == division ** 1/2:
#             return raiz
         
# def tiempo_caida_libre(altura: int, masa: int) -> str:
#     resultado= 'round(x,2)'
#     def tiempo_caida_libre(raiz):
#          if resultado == round(raiz, 2):
#              return resultado


altura = 10
masa = 85
 
def tiempo_caida_libre(altura: int, masa: int) -> str:
    gravedad = 9.807
    
    tiempo = round(((altura*2) / gravedad) ** (1/2),2)
    return ("El tiempo que tarda un balon de "+str(masa)+"kg en caida libre desde una altura de "+str(altura)+" m en impactar en el suelo es de: "+str(tiempo)+"seg")

# print(multiplicar)
# print(division)
# print(raiz)

















